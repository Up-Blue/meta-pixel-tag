# meta-pixel-tag
Browser-side Meta Pixel tag for Google Tag Manager that automatically maps GA4 e-commerce dataLayer events to Meta (Facebook) standard events. Supports both automatic and manual event configuration, custom data parameters, and user data matching. Built by Up Blue (upblue.pl).
